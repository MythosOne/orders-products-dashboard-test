{
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only server/server.ts"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.2.1",
    "@types/socket.io": "^3.0.1",
    "ts-node-dev": "^2.0.0"
  }
}
